
const abi = [
  // runcommands
  "function runCommands(uint[])",
  
  // run saved command
  "function runSavedCommand(uint) view returns (uint)",
  // process
  "function process(uint,bytes4,bytes)",
  
	];
export default abi;

